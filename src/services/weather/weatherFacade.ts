/**
 * weatherFacade.ts — Service-layer re-exports for weather domain.
 * Routes component imports away from direct @/core/weather access.
 */

export type { SimWeatherPattern, WeatherState } from "@/core/weather";
