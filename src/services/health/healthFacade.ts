/**
 * healthFacade.ts — Service-layer re-exports for health domain.
 * Routes component imports away from direct @/core/health access.
 */

export { formatRecoveryDays } from "@/core/health/injuryDisplay";
