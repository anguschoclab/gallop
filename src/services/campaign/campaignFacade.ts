/**
 * campaignFacade.ts — Service-layer re-exports for campaign domain.
 * Routes component imports away from direct @/core/campaign access.
 */

export type { AutoRegisterEntry, AutoRegisterResult } from "@/core/campaign/autoRegister";
