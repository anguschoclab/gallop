/**
 * dashboardFacade.ts — Service-layer re-exports for dashboard domain.
 * Routes component imports away from direct @/core/dashboard access.
 */

export { deriveNextAction } from "@/core/dashboard/nextAction";
