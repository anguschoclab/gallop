/** Minimum valid gate number (gates are 1-indexed). */
export const MIN_GATE = 1;

/** Default gate position when none has been assigned. */
export const DEFAULT_GATE = 1;

/** Days before a G1 race to perform the gate draw (with email notification). */
export const G1_GATE_DRAW_LEAD_DAYS = 5;

/** Days before a non-G1 race to perform the gate draw (silent). */
export const STANDARD_GATE_DRAW_LEAD_DAYS = 2;

/** Fallback value for sorting entries with undefined gate (sorts first). */
export const GATE_SORT_FALLBACK = 0;
