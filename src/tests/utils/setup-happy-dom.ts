// @ts-ignore — @happy-dom/global-registrator is a devDependency for bun-native tests only (not used by vitest)
import { GlobalRegistrator } from "@happy-dom/global-registrator";

GlobalRegistrator.register();
