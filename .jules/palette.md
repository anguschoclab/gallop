## 2024-05-04 - Adding ARIA labels to icon-only buttons
**Learning:** Icon-only buttons often lack proper accessibility attributes in this app, specifically relying only on `title` instead of a proper `aria-label`.
**Action:** When adding or modifying icon-only buttons, always ensure an `aria-label` attribute is present alongside any `title` attribute for better screen reader support.
