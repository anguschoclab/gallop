## 2024-05-18 - Flavor Stories Enrichment
**Learning:** Adding new sentences and flavor texts to existing arrays like `FLAVOR_STORIES` is a safe, zero-logic way to enrich the game's atmosphere and variance.
**Action:** Append 5-10 new stories to categories like `track`, `jockeys`, `weather`, and `community` in `src/core/narrative/flavorStories.ts`, ensuring they use `category: "flavor"` and generic roles.
