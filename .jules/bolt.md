- O(N) Array methods like `find` and `findIndex` inside nested loops over large datasets (e.g., thousands of horses in `npcRaceEntry.ts`) cause severe performance bottlenecks.
- Replacing these inner loop array scans with O(1) Map lookups generated outside the loop can yield ~30-40x performance improvements in critical path game loops.
