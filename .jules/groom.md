## 2025-05-18 - Clear Filters Button in Gallery

**Learning:** When users apply multiple filters (coat, trait, search) on lists (like Horse Gallery), ending up with 0 results leaves them in a dead end without a clear way to reset their view.
**Action:** Always provide a "Clear Filters" button in empty states caused by filtering, so users don't have to manually reset every select dropdown or search input.
